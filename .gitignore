# Build output (raw Makefile tree). Released binaries live in bin/ and ARE committed.
build/
.dep/
*.o
*.elf
*.map
*.lst
*.hex
*.list
*.dmp
*.stackdump

# Pristine upstream baseline, kept locally for diffing only
reference/

# Editor / OS
*~
.DS_Store
TAGS
.emacs-dirvars
.gitignore

# Claude Code local config (not part of the firmware)
CLAUDE.md
.claude/
